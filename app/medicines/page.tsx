import SectionPage from "@/components/SectionPage";import { medicineGroups } from "@/components/marketplace-data";export default function Page(){return <SectionPage eyebrow="Health essentials" title="Medicines" intro="Request medicines and wellness supplies with assisted ordering. Prescription medicines will require a valid prescription before fulfilment." items={medicineGroups}/>}
